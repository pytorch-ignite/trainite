[project]
name = "counting"
version = "0.1.0"
description = "counting - generated by Trainite"
dependencies = [
    "PyYAML",
    "omegaconf>=2.3.0",
    "pydantic>=2",
    "pytorch-ignite",
    "tensorboard",
    "torch",
    "clearml",
]
requires-python = ">=3.10"
